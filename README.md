# 📸 Windows 11 全局备忘录工具

**当前版本**: v2.1.0

一个基于 Electron 的 Windows 11 本地常驻托盘工具，支持全局快捷键触发自动全屏截图、保存截图与剪贴板内容，并提供智能侧边栏抽屉模式和强大的记录管理功能。

## ✨ 核心功能

### 📸 截图功能
- **托盘常驻**：应用在系统托盘常驻运行
- **全局快捷键**：默认 `Ctrl+Shift+X` 触发截图并可在配置中修改
- **全屏截图**：自动捕获当前操作界面内容并保存
- **多屏幕支持**：根据鼠标当前所在的显示器自动截取全屏画面
- **剪贴板保存**：同时读取并保存当前剪贴板内容（文本/图片）
- **元数据记录**：自动生成包含时间戳、路径、剪贴板内容的 JSON 元数据文件

### 🗂️ 智能侧边栏 (Sidebar Drawer)
- **抽屉式设计**：Memo 窗口支持停靠在屏幕边缘，收起时仅显示一个小把手
- **平滑动画**：支持毫秒级的滑动展开/收起动画效果
- **全方位记忆**：精准记忆把手位置、展开后的窗口尺寸和坐标，重启依然有效
- **交互逻辑**：
  - **左键单击**：立即展开窗口
  - **右键拖拽**：在屏幕边缘自由移动把手位置
  - **自动收起**：点击窗口外部（失焦）或 Pin 按钮时自动归位

### 🪟 智能浮窗
- **快速操作**：截图完成后在鼠标附近弹出操作浮窗
- **编辑 Note**：支持添加和编辑文本笔记
- **自动保存**：Note 内容自动保存，无需手动操作
- **剪贴板预览**：显示文本摘要或图片缩略图

### 📝 记录管理（Memo）
- **全部记录查看**：统一界面管理所有截图和笔记
- **状态管理**：支持 Todo/Done 状态切换和筛选
- **拖拽排序**：自由调整笔记顺序
- **新建笔记**：支持创建纯文本 Note（无截图）
- **缩略图预览**：支持快速查看截图和剪贴板图片

### 🎨 视觉设计
- **现代 UI**：渐变背景、磨砂玻璃效果
- **自定义图标**：统一的图标系统（ICO 和 PNG 格式）
- **无边框设计**：macOS 风格的控制按钮
- **流畅动画**：按钮悬停、点击、窗口滑动等交互动画
<img width="1554" height="505" alt="image" src="https://github.com/user-attachments/assets/26405bad-1b36-45ff-8bb6-1a0b1d3c3f2f" />

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动应用

```bash
npm start
```

## 📋 使用说明

### 截图操作

1. 启动应用后，会在系统托盘显示图标
2. 按下快捷键 `Ctrl+Shift+X` 触发截图（支持多显示器自动判定）
3. 截图完成后会在鼠标附近弹出浮窗
4. 在浮窗中可以：
   - 编辑 Note 文本（自动保存）
   - 查看剪贴板内容预览
   - 点击 Save 保存当前内容
   - 点击 Open Memo Window 打开记录管理窗口

### Sidebar Drawer 侧边栏

1. Memo 窗口默认以侧边栏模式运行
2. **展开**：左键单击屏幕右侧的小把手
3. **移动把手**：右键按住把手并上下左右拖动
4. **收起**：点击窗口外部或标题栏上的 Pin 按钮
5. **调整尺寸**：展开状态下拖动窗口左边缘，系统会自动记忆新的尺寸

### 托盘菜单

右键点击托盘图标可以：
- Screenshot (Ctrl+Shift+X) - 执行截图操作
- Memo Window - 打开记录管理窗口
- Settings - 进入配置中心
- Exit - 退出应用

## 📁 文件保存位置

所有截图和元数据文件保存在：

```
C:\Users\<用户名>\AppData\Roaming\screendemo\captures\
```

### 文件命名规则

- **截图文件**：`capture_<日期时间>_<时间戳>.png`
- **元数据文件**：`capture_<日期时间>_<时间戳>.json`

## ⚙️ 配置说明

通过托盘菜单进入 **Settings** 界面，可修改以下参数：

- **Global Shortcut**：全局截图快捷键
- **Save Location**：自定义保存目录
- **Sidebar Drawer Settings**：
  - 启用/禁用侧边栏模式
  - 自定义把手宽度
  - 自定义展开宽度
  - 自动收起延迟设置
  - 停靠屏幕选择（跟随鼠标或主屏幕）

详细更新内容请查看 [CHANGELOG.md](CHANGELOG.md)。

## v2.1.0 新功能

### ✨ 智能侧边栏抽屉模式
- 全新的侧边栏交互体验，减少桌面空间占用
- 实现毫秒级平滑滑动动画
- 引入“双态位置记忆”，把手和展开状态各司其职

### 🖥️ 深度多屏幕适配
- 截图不再局限于主屏，自动跟随光标焦点
- 修复多屏环境下窗口坐标越界的稳定性问题

详细变更记录请查看 [CHANGELOG.md](CHANGELOG.md)。

## 🛠️ 技术栈

- **Electron** - 跨平台桌面应用框架
- **screenshot-desktop** - 屏幕截图库
- **Node.js** - JavaScript （运行时）

## 📄 许可证

MIT License

---

**提示**：首次运行时，Windows 可能会弹出防火墙提示，请允许应用访问网络（尽管本应用不需要网络功能，但 Electron 可能会请求）。
